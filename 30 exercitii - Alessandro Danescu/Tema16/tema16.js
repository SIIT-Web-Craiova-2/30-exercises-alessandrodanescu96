function sumTriple (x, y) {
  if (x == y) {
    return 3 * (x + y);
    }
   else
   {
    return (x + y);
   }
 }
console.log(sumTriple(15, 30)); // x nu e egal cu y si se face a 2-a comanda
console.log(sumTriple(20, 20)); // x = y si se executa prima comanda
